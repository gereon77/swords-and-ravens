{
  "name": "agot-bg-website-client-assets",
  "private": true,
  "version": "1.0.0",
  "description": "Tailwind CSS + DaisyUI build for the Swords and Ravens ASP.NET Core website (agot-bg-website). Builds ../wwwroot/css/app.css, which is committed like any other static asset — see ../../README.md.",
  "scripts": {
    "build": "tailwindcss -i ./src/app.css -o ../wwwroot/css/app.css --minify",
    "watch": "tailwindcss -i ./src/app.css -o ../wwwroot/css/app.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "daisyui": "^5.7.32",
    "tailwindcss": "^4.3.3"
  }
}
